const merchantServiceModel = require('../models/merchantService.js');
const fs = require('fs');

class controllerMerchants{
    static getMerchantByName(req, res){
    }
}

module.exports = controllerMerchants;